在Qt Designer中设置信号与槽

信号(signal) 槽(slot) 是Qt的核心机制

信号: 由对象或控件发送出来的消息

按钮的单击事件

当单击按钮时，按钮就会向外部发送单击的消息

这些发送出去的信号需要一些代码来拦截，这些代码就是槽

槽 本质上是一个函数或者方法

信号可以理解为时间

槽可以理解为事件函数

需要将信号和槽绑定

一个信号可以和多个槽绑定

一个也可以拦截多个信号

操作方法：在Edit的信号与槽编辑当中，然后点中控件往下拖拽，再松开，之后选择需要连接的信号